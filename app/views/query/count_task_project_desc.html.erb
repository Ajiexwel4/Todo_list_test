<h5>The count of all tasks in each project, order by tasks count descending</h5>

<table class= "table-bordered">
  	<thead>
		<tr>
			<th>Project</th>
			<th>tasks count</th>
		</tr>
  	</thead> 
	<tbody>
<% @projects.each do |project| %>
    <tr>
      <td><%= project.name %></td>
      <td><%= project.tasks.count %></td>
        
    </tr>
<% end %>
    </tbody>
</table>
<br>


<%= link_to 'Back', tasks_path %>